# name of microservice
spring:
  application:
    name: catalog-service
  # we specify that it take the configurations of the config-service
  could:
    config:
      dicovery:
        enamble: true
        service-id: config-service
  # profile of microservice
  profiles:
    active: dev
